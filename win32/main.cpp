
#include "..\include\RayTracer.h"

int main(int argc, char** argv)
{
	//RAY::Scene* scene = new RAY::Scene;

	//RAY::Shape* sh1 = new RAY::SHAPE::Sphere(1.6f);
	//RAY::Shape* sh2 = new RAY::SHAPE::Cube(2.0f);//RAY::SHAPE::Sphere(1.6f);
	//RAY::Shape* sh3 = new RAY::SHAPE::InvSphere(8.0f);
	//RAY::Light* p1 = new RAY::Light();
	//RAY::Light* p2 = new RAY::Light();
	//scene->renderable.add(sh1);
	//scene->renderable.add(sh2);
	//scene->renderable.add(sh3);
	//scene->lights.add(p1);
	////scene->lights.add(p2);
	//scene->build();

	//scene->mainCamera->transform->translate(5.6f, 0.8f, -5.6f);
	//scene->mainCamera->transform->rotate(-5.0f, 45.0f, 0.0f);
	//scene->mainCamera->focalDepth = 4.0f;
	//sh1->transform->translate(0.0f, 2.4f, 0.0f);
	//sh2->transform->translate(0.0f, -2.4f, 0.0f);
	//sh2->transform->rotate(0.0f, 45.0f, 0.0f);
	//sh1->material->overlay = COLOR(1.0f, 0.0f, 0.0f);
	//sh2->material->overlay = COLOR(0.0f, 0.0f, 1.0f);
	//sh3->material->overlay = COLOR(0.0f, 1.0f, 0.0f);
	//p1->transform->translate(1.6f, -0.8f, -2.0f);
	//p1->falloff = vec3(0.01f, 0.06f, 0.25f);
	////p2->transform->translate(-3.2f, 4.0f, 0.0f);

	//scene->prepare();
	//RAY::Photo* photo = scene->render();
	//photo->flatten();
	//photo->package();
	//delete photo;

	//scene->unpack();
	//delete scene;

	//xml_document<> doc;
	//doc.parse<0>("");

	return 0;
}